import {Common} from '../../common/untils';

let urlParams = Common.getRequest();

/**
 * @param [userId] um账号
 * @param [certificateNo] 员工身份证号
 * @param [title] 标题
 * @param [routeCount] 路由长度
 *
 */
export default {
  userId: urlParams.userId || '',
  certificateNo: urlParams.certificateNo || '',
  title: '理赔服销',
  routeCount: 0
};
